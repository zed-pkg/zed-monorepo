# Template only. Real secret created out-of-band (see .gitignore: *.secret.yaml).
#   kubectl -n zed create secret generic zed-web-server-secrets \
#     --from-literal=DATABASE_URL='postgres://zed:PASSWORD@postgres:5432/zed'
apiVersion: v1
kind: Secret
metadata:
  name: zed-web-server-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgres://zed:REPLACE-ME@postgres:5432/zed"
