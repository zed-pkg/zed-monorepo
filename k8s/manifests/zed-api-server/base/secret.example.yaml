# Template only. Do NOT commit the real secret (see .gitignore: *.secret.yaml).
# Create the real one out-of-band, or manage it with SOPS / External Secrets:
#
#   kubectl -n zed create secret generic zed-api-server-secrets \
#     --from-literal=DATABASE_URL='postgres://zed:PASSWORD@postgres:5432/zed' \
#     --from-literal=AWS_ACCESS_KEY_ID='R2_ACCESS_KEY' \
#     --from-literal=AWS_SECRET_ACCESS_KEY='R2_SECRET_KEY'
apiVersion: v1
kind: Secret
metadata:
  name: zed-api-server-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgres://zed:REPLACE-ME@postgres:5432/zed"
  AWS_ACCESS_KEY_ID: "REPLACE-ME"
  AWS_SECRET_ACCESS_KEY: "REPLACE-ME"
