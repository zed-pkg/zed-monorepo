# Project tracking changelog

## 2026-08-05

- Added the version-controlled organization delivery record.
- Recorded merged evidence for `zed-api-server.rs` PRs #11 and #13.
- Recorded the semantically reconstructed three-driver browser harness, independent release-report consumer, collision-safe shared-stack allocation, and real-browser Rust/WASM certification.
- Recorded the merged certified-package artifact publication workflow in `zed-interfaces` PR #42.
- Added the `zed-pkg` and `zed-pkg-test` organization-to-project mapping and the default `<organization>-project` / `github.com/<organization>` convention.
- Added the project promotion procedure, exact candidate/merge SHA fields, retained-artifact evidence, and semantic conflict-resolution requirements.
- Added the machine-readable `zed-pkg/project-tracking/v1` schema.
- Linked Linear issue `DEN-2211` as the acceptance record for this documentation tranche.
